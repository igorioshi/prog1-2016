#include <stdio.h>

int ehPar(int num, int resto)
{
    resto = num % 2;
    
    if (resto == 0)
    {
        
    }
}

int main()
{
    int imparPar;
    int num;
    
    printf("\nEntre com um numero: ");
    scanf("%d", &num);
    
    imparPar = ehPar(num, imparPar);
}